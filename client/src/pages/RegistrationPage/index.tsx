import { FC } from 'react';
import AuthForm from '../../components/shared/AuthForm';

const RegistrationPage: FC = () => {
  return <AuthForm template="registration" />;
};

export default RegistrationPage;
